#pragma once
#include <GL/glut.h>
#include <stdio.h>

class FullScreen {
                  public:
					     static bool gamemode;
	                     static void habilitar_FULL();
				         static void deshabilitar_FULL();
};

