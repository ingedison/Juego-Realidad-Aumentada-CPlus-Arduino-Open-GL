#pragma once
#include "Dibujar.h";

class Viewport {
				public:
			    static int width;
				static int height;
				static const float WALKING_SPEED;
				static void ini();
				static void reshape(int w, int h);
				static void show();
				
				//PANTALLA COMPLETA - FULL
				
};

